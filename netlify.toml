[build]
  command = "npx expo export -p web && node scripts/inject-web-icons.mjs"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Single-page app: serve index.html for any route so deep links don't 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
